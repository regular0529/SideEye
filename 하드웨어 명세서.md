# 웨어러블 하드웨어 자원 명세서

> 최종 갱신: 2026-08-04  
> 기준: 현재 보유 장비와 확인된 동작 상태. 목표 사양이나 수업 예제는 보유·검증 장비로 취급하지 않는다.

---

## 1. 범위와 상태 기준

이 문서는 웨어러블 프로젝트에서 실제로 보유한 하드웨어와 검증 수준을 관리하는 기준 문서다. ESP-NOW 실습 장비, 라즈베리파이 등 수업 예제 장비, 아직 보유하지 않은 부품은 본문 구성에서 제외한다.

| 상태 | 의미 |
| --- | --- |
| 보유 | 실물 보유만 확인됐다. 연결 또는 기능 동작을 뜻하지 않는다. |
| 연결 확인 | 전원, 버스, 장치 식별 또는 통신이 확인됐다. |
| 기능 검증 | 프로젝트에서 사용할 실제 기능과 측정값 또는 출력이 확인됐다. |
| 계획 | 아직 보유하지 않았거나, 현재 구성에서 동작을 검증하지 않았다. |

## 2. 현재 개발 우선순위

| 대상 | 현재 구성 | 상태 |
| --- | --- | --- |
| 센서 웨어러블 MVP | 일반 XIAO ESP32-S3 + BNO055 + BME280 + BH1750 | 최우선. 센서 3종의 실측값까지 확인됨. |
| 비전 AI 프로토타입 | XIAO ESP32-S3 Sense 1세트 | 카메라 캡처, 웹 미리보기, 온디바이스 이미지 분류가 검증됨. |
| 센서와 비전의 최종 통합 | 두 MCU의 전원, 통신, 착용 구조 | 미정. 현 시점에서 단일 완성 웨어러블 구조로 가정하지 않는다. |

## 3. 보유 하드웨어 현황

| 장비 | 수량 | 상태 | 현재 용도 또는 검증 근거 |
| --- | ---: | --- | --- |
| Seeed Studio XIAO ESP32-S3 | 1 | 기능 검증 | 센서 웨어러블 MVP의 컨트롤러. BNO055, BME280, BH1750을 한 I2C 버스에서 사용한다. |
| GY-BNO055 9축 IMU | 1 | 기능 검증 | 낙상 및 이상 상태 감지용. 실제 측정값이 정상임을 확인했다. |
| BME280 환경 센서 | 1 | 기능 검증 | 온도, 습도, 기압의 환경 기록용. 실제 측정값이 정상임을 확인했다. |
| BH1750 조도 센서 | 1 | 기능 검증 | 조도 기반 자동 밝기 또는 모드 제어용. 실제 측정값이 정상임을 확인했다. |
| XIAO ESP32-S3 Sense 키트 | 1세트 | 기능 검증 | 전용 카메라 확장보드를 결합한 비전 AI 개발 보드. 카메라 캡처와 온디바이스 추론을 확인했다. PDM 마이크는 현재 프로젝트 범위에서 제외한다. |
| Seeed Studio Round Display for XIAO | 1 | 보유 | 센서 웨어러블의 화면 및 자동 밝기 UI 후보. 화면, 터치, RTC, microSD 기능은 아직 검증하지 않았다. |
| microSD 카드 | 4 GB, 1 | 보유 | 데이터 로깅 후보. 현재 마운트 및 파일 읽기/쓰기는 미검증이다. |
| ESP32 DevKit 계열 보드 + 쉴드 | 1세트 | 보유 | 별도 실험 장비. 보드 모듈명, 쉴드 기능, 핀 구성, 동작 상태가 아직 확인되지 않았다. 핵심 웨어러블 구성에는 포함하지 않는다. |

## 4. 센서 웨어러블 MVP

### 컨트롤러와 I2C 구성

일반 XIAO ESP32-S3에서 세 센서를 동시에 사용한다. 현재 검증 펌웨어는 `WearableTimeSeriesAP/WearableTimeSeriesAP.ino`이다.

| 항목 | 값 |
| --- | --- |
| I2C SDA | `D4 / GPIO5` |
| I2C SCL | `D5 / GPIO6` |
| BNO055 | `0x29`, chip ID `0xA0` |
| BME280 | `0x76`, chip ID `0x60` |
| BH1750 | `0x23` |
| 확인 기록 | 2026-08-04 COM12 I2C 스캔 및 현재 측정값 확인 |

### 센서별 제품 역할

| 센서 | 확정 역할 | 구현 시 유의점 |
| --- | --- | --- |
| BNO055 | 낙상·이상 감지 | 현재 `0x29` 주소를 사용한다. 일반 클론 모듈은 외부 32 kHz 크리스털이 없을 수 있으므로, 검증된 구성에서는 내부 오실레이터를 유지한다. |
| BME280 | 온도·습도·기압 환경 기록 | BME280 chip ID `0x60`을 확인한 구성이다. |
| BH1750 | 주변 조도 기반 자동 밝기·모드 제어 | 주변 조도 변화에 따라 값을 재확인하는 수동 시험이 필요하다. |

센서 웨어러블은 현재 1차 MVP다. Round Display는 UI 목표에 포함하지만, Display 자체가 기능 검증되기 전까지는 이 MVP의 동작 의존성으로 간주하지 않는다.

## 5. XIAO ESP32-S3 Sense 비전 AI 프로토타입

### 검증된 구성

- 펌웨어: `CameraInference/CameraInference.ino`
- 보드 설정: `esp32:esp32:XIAO_ESP32S3`, `PSRAM=opi`
- 카메라 캡처: `240x240` JPEG, PSRAM 프레임 버퍼 사용
- 검증된 추론: Edge Impulse 기반 단일 라벨 이미지 분류
- 현재 검증 모델: `background`, `mouse`, `tumbler` 3개 클래스, `96x96` 입력
- 개발용 기능: 브라우저 미리보기, 최신 프레임 조회, 분류 결과 조회, 데이터 수집

현재 모델은 비전 파이프라인과 보드 성능을 검증하기 위한 모델이다. 최종 웨어러블에서 무엇을 인식할지는 아직 결정되지 않았으므로, 이 모델을 최종 제품 기능으로 기록하지 않는다. 이 모델은 객체 탐지가 아니라 프레임 전체를 분류하는 이미지 분류 모델이다.

### 카메라 모듈 주의사항

Sense 키트의 카메라 센서 세부 모델은 실물 표기로 아직 확인하지 않았다. 따라서 이 문서에서는 OV2640 또는 OV3660 중 하나로 단정하지 않는다. 카메라 센서 모델, 해상도, 전력 요구사항이 필요해지면 확장보드의 실물 표기를 먼저 확인한다.

## 6. 보유·계획·미상 항목의 처리

| 항목 | 현재 판단 | 다음 조치 |
| --- | --- | --- |
| Round Display | 보유만 확인 | 화면 출력, 터치, RTC, microSD를 각각 독립적으로 시험한 뒤 상태를 올린다. |
| microSD 카드 | 보유만 확인 | 카드 마운트와 파일 읽기/쓰기를 확인하기 전에는 데이터 로깅 저장장치로 사용한다고 기록하지 않는다. 2026-08-03 Sense Probe의 SD 마운트 실패는 결선 또는 카드 연결이 불완전했을 가능성이 있어 카드 불량 판정 근거가 아니다. |
| 배터리 | 계획 | 아직 보유 장비가 아니다. 독립 착용형 기기로 명세하지 않으며, 도입 시 용량, 충전 방식, 목표 사용 시간을 별도로 결정한다. |
| ESP32 DevKit 계열 보드 + 쉴드 | 모델·기능 미상 | 기판 표기, 전압, 핀맵, 쉴드 기능을 확인할 때까지 실험 장비로만 기록한다. |
| 센서와 Sense 통합 | 미정 | 최종적으로 한 기기에 넣을지, 독립 프로토타입으로 유지할지 결정한 뒤 전원·통신·기구 설계를 추가한다. |

## 7. 검증 근거와 갱신 규칙

| 근거 | 확인 내용 |
| --- | --- |
| `WearableTimeSeriesAP/README.md` | 세 I2C 센서의 주소와 chip ID, XIAO I2C 핀, 동시 센서 대시보드 구성 |
| `WearableTimeSeriesAP/WearableTimeSeriesAP.ino` | `D4/D5` I2C 초기화와 `0x29`, `0x76`, `0x23` 센서 구성 |
| `PROJECT_RECORD.md` | Sense의 Edge Impulse 이미지 분류 모델, PSRAM 설정, 카메라 추론 검증 기록 |
| `CameraInference/CameraInference.ino` | Sense 카메라 핀, PSRAM 버퍼, 웹 미리보기와 온디바이스 추론 구현 |
| `WearableProbe/README.md` | Sense 확장보드의 과거 SD 마운트 미성공 기록과 재시험 조건 |

상태를 `연결 확인` 또는 `기능 검증`으로 변경할 때는 시험 날짜, 사용한 보드, 펌웨어 또는 스케치 경로, 확인한 결과를 함께 남긴다. 네트워크 SSID, 비밀번호, API 키 같은 비밀값은 이 하드웨어 명세서에 기록하지 않는다.
