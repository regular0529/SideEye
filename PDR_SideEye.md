# PDR: SideEye (사이드아이)

**주제**: 초경량 엣지 IMU 조향 의도 감지 및 비전 AI 기반 모빌리티 후측방 경보 웨어러블 디바이스 (Personal Mobility BCW)
**제품명**: SideEye (사이드아이)

**버전**: v2 (2026-08-05, 팀 분업/단계적 로드맵 반영 확정판)
**전신 문서**: `PDR_헬멧_사각지대_경고시스템.md` (기능 설계는 동일, 이 문서가 팀 분업·로드맵·발표 연동을 추가한 최종판)

> 부산대 웨어러블 AI 엣지 컴퓨팅 특강 팀 프로젝트 제출물. 배경/탈락한 대안/평가기준 분석은 `COURSE_UNDERSTANDING_AND_WINNING_STRATEGY.md`, `TOPIC_SELECTION_RECORD.md` 참조. 이 문서는 **실제로 만들 것과 누가 무엇을 언제까지 하는지**만 다룬다.

---

## 1. 한 줄 정의

자전거·킥보드 헬멧 좌우에 XIAO ESP32S3 Sense를 하나씩 부착해, IMU로 몸의 기울임(조향 의도)을 감지하는 즉시 해당 방향 카메라가 차량 유무를 확인하고 LED 점멸 + 부저로 경고하는 **엣지 우선 + 단계적 연결성** 웨어러블. 인터넷 없이도 항상 동작하는 것이 최우선 원칙.

## 2. 확정된 기능 스펙 (열린 결정 없음)

이전 버전(`PDR_헬멧_사각지대_경고시스템.md` 14절)의 미확정 사항을 전부 확정했다. 아래는 최종 스펙이며 재논의 없이 그대로 구현한다.

| 항목 | 확정 내용 | 사유 |
|---|---|---|
| 카메라 클래스 | **2클래스**(`vehicle` / `background`) | 기존 프로젝트 94.6% 검증된 방식 재사용, 48시간 내 정확도 리스크 최소화 |
| 부저 방향 인코딩 | **방향 구분 없는 단일 비프음**. 방향 표시는 LED가 전담 | 좌/우 기울임은 물리적으로 동시에 발생 불가 → 부저에 방향 정보를 실을 필요 자체가 없음. LED 5회 점멸이 이미 방향을 명확히 표시 |
| 발표 프레이밍 | **실전 안전 문제 해결형** (재미 요소 지양) | 안전 문제는 심사위원에게 진지하게 받아들여지고, "완성도 최우선" 팀 전략과 일치 |

## 3. 시스템 동작 — 팀 전원의 공통 이해 기준

아래 시퀀스가 이 프로젝트의 유일한 정답 동작이다. 모듈을 나눠 각자 개발해도 이 흐름과 어긋나면 안 된다.

**핵심 원칙: 네오픽셀(방향지시등)과 부저(위험경보)는 서로 다른 트리거로 분리된 별개 출력이다.**

```
[마스터 보드] BNO055 IMU 상시 저전력 분류 (idle / left-lean / right-lean), 5~10ms 주기
   │
   ├─ idle → 아무 일도 없음 (네오픽셀 꺼짐, 카메라 항상 꺼져있음)
   │
   └─ left-lean 또는 right-lean 감지
         │
         ├─ [즉시, 무조건] 마스터 네오픽셀 → 해당 방향 색상 점등 (조향 의도 표시, 방향지시등 기능)
         │     차량 감지 여부와 무관하게 기울이면 항상 켜짐 — 실제 차량 방향지시등과 동일한 개념
         │
         └─ [병행] 카메라 캐스케이드 트리거
               ├─ 그 방향 보드가 마스터 자신이면 → 마스터가 직접 카메라 캡처 + 분류
               └─ 그 방향 보드가 슬레이브면 → ESPNOW로 CMD_CAPTURE_REQUEST 전송
                                                └─ 슬레이브: 카메라 캡처 + 분류 → ESPNOW로 CMD_RESULT_REPLY 회신
               │
               └─ 결과 확보 (로컬 분류 또는 ESPNOW 회신)
                     ├─ vehicleFound == false → 부저 없음 (네오픽셀은 방향지시등이라 계속 켜져 있음)
                     └─ vehicleFound == true  → 마스터 부저 단일 비프 (위험 경보 전용, 네오픽셀과 독립)
```

**불변 조건**: 한 시점에 활성화되는 카메라는 항상 하나뿐(몸이 동시에 좌우로 기울 수 없으므로). 카메라는 트리거 전까지 상시 꺼져있다(상시 촬영 절대 금지 — 배터리/버벅임 리스크의 근본 원인이므로). 부저는 항상 마스터 보드에서만 울린다 — 슬레이브는 감지만 하고 표시/발음하지 않는다. 네오픽셀은 항상 마스터에만 있으므로 좌/우 모두 마스터가 켠다(슬레이브는 카메라 결과만 회신).

**네오픽셀 색상 규약** (방향지시등 + 후미등 겸용, 모듈 A 구현, 6.4 참조):

| 상태 | 색상 | 트리거 |
|---|---|---|
| 좌측으로 기울임 | 빨강(`0xFF0000`) | IMU 좌측 감지 즉시, 차량 유무 무관 |
| 우측으로 기울임 | 파랑(`0x0000FF`) | IMU 우측 감지 즉시, 차량 유무 무관 |
| 정지등(브레이크, **다음 단계 로드맵**) | 흰색(`0xFFFFFF`) | 감속 감지(별도 분류기 필요, 이번 MVP 범위 밖). 좌측 색상(빨강)과 겹치지 않게 별도 지정 |
| 대기 | 꺼짐 | idle |

부저는 색상 규약과 무관 — 오직 `vehicleFound == true`일 때만 단일 비프. 좌/우 색상은 팀 실측 시 시인성 기준으로 최종 확정(임시값).

### 3.1 전력 상태 3단계

| 상태 | 조건 | MCU/센서 동작 |
|---|---|---|
| Deep sleep | 헬멧 미착용(오프 바디) | MCU 대부분 정지, IMU 웨이크업 조건(움직임)까지 최저전력 |
| Light sleep | 헬멧 착용, 정지 상태 | IMU만 상시 저전력 폴링(5~10ms), 카메라 완전 꺼짐, 무선(ESPNOW) 대기 |
| 주행(라이딩) | IMU가 지속적 움직임 감지 | Light sleep과 동일하게 카메라는 트리거 전까지 꺼져있음 — 캐스케이드 원칙 유지 |

카메라를 "완전 오프 → 트리거 시 콜드스타트"로 할지, "웜 대기(센서 초기화 유지, 프레임만 온디맨드 캡처)"로 할지는 **미확정 — 실측 후 결정**. `esp_camera_init()` 콜드스타트 자체의 지연시간이 아직 벤치마크 안 됨(기존 180ms는 카메라가 이미 초기화된 상태에서의 캡처+추론 시간만 잰 것, `PROJECT_RECORD.md` 참조). 체크포인트 5(비전 학습/캐스케이드 통합) 단계에서 두 방식 모두 실측하고, 반응속도(콜드스타트 오버헤드) vs 대기전력 트레이드오프를 비교해 결정한다. 목표는 여전히 "왕복 체감 지연 없음(<1초)"이며, 콜드스타트가 이 목표를 못 맞추면 웜 대기로 전환.

## 4. 하드웨어 구성

| 보드 | 위치 | 부착 부품 |
|---|---|---|
| 마스터 | 헬멧 우측 | XIAO ESP32S3 Sense, BNO055(IMU, I2C: SDA=GPIO5, SCL=GPIO6), 부저 1개(GPIO 직결, `tone()`), 네오픽셀 링(WCMCU-2812B-12, 12구, D1/GPIO2 직결) |
| 슬레이브 | 헬멧 좌측 | XIAO ESP32S3 Sense, 내장 LED(경보 표시 미사용, 디버깅 전용) |

> **해결됨 (2026-08-05)**: 처음엔 `Adafruit_NeoPixel` + ESP32 코어 3.2.0 조합에서 RMT 채널의 `allow_pd` 플래그가 초기화 안 돼 전송 자체가 실패했다(전원/배선은 처음부터 다 정상이었음 — 오진단으로 "모듈 사망"이라 결론 내렸다가 번복). ESP-IDF `driver/rmt_tx.h`로 직접 제어(`allow_pd = 0` 명시)하는 방식으로 우회해 해결. 상세 진단 기록은 `NEOPIXEL_DEBUGGING.md`, 검증 코드는 `firmware/master/neopixel_direct_rmt_test/`. 12구라 USB 전류 한계 때문에 밝기는 64/255로 제한.
- 카메라는 각 보드가 바깥쪽(주행 방향 옆)을 향하도록 장착.
- PSRAM `OPI` 필수 활성화(카메라 추론 텐서 아레나용) — `ESP32_Wearable_Hardware_Spec.md`, `PROJECT_RECORD.md`에 이미 검증된 빌드 옵션 재사용.

## 5. 모듈 분할 — 병렬 개발용 (담당자 미정, 구조만 확정)

> 팀 구성(누가 어느 모듈을 맡을지)은 추후 확정. 아래는 **기능 기준 5개 독립 모듈**이며, 각자 자신의 LLM으로 독립 개발 가능하도록 인터페이스를 고정한다. 모듈 간 통신 계약은 6절의 구조체/API로 못박아 두었으니, 이 계약만 지키면 서로의 진행 상황과 무관하게 병렬 작업 가능하다.

| 모듈 | 범위 | 산출물 위치 (예정) | 다른 모듈과의 계약 |
|---|---|---|---|
| **A. 마스터 통합** | IMU 분류 + ESPNOW 마스터측 송수신 + 경보(네오픽셀/부저) 로직 총괄. 가장 복잡한 통합 지점이므로 프로젝트/MCU 경험자 배치 권장 | `firmware/master/` | 6.1 IMU 분류 API, 6.2 ESPNOW 메시지 구조체 정의자(다른 모듈이 이 정의를 import) |
| **B. 슬레이브 보드** | ESPNOW 요청 수신 → 카메라 캡처+분류 → 결과 회신. 경보 표시는 마스터가 전담하므로 자체 LED 제어 불필요(디버깅용 내장 LED만 옵션) | `firmware/slave/` | 6.2 메시지 구조체 소비, 6.3 카메라 분류 함수 시그니처 소비 |
| **C. 비전 학습 파이프라인** | Edge Impulse 데이터 수집·학습·Arduino 라이브러리 배포. 모듈 A/B 양쪽에 동일 모델 제공 | `vision_harness/`, Edge Impulse 프로젝트 | 6.3 카메라 분류 함수 시그니처를 A/B에 제공(입력: 96x96 프레임, 출력: `vehicleFound` bool + confidence) |
| **D. 데이터 수집 + 통합 테스트** | 실외 차량 촬영 로지스틱스, IMU 좌우 기울임 반복 동작 수집, 8절 체크포인트별 수용 기준 실제 실행/기록 | `dataset/`, 테스트 로그 | 모든 모듈의 산출물을 8절 체크포인트 기준으로 검증, 실패 시 담당 모듈에 회귀 리포트 |
| **E. BLE/웹 로깅 (보너스)** | 2단계 통신(폰 연결 시 로그 전송), 데모 보조 대시보드 | `firmware/ble_web/` | A의 IMU/경보 이벤트를 구독만 함. **A/B/C가 끝나기 전까지 착수하지 않음** — 미완성이어도 1단계 데모에 영향 없음 (9절 참조) |

데이터 수집/발표자료 조사처럼 코딩이 아닌 작업은 모듈 D의 확장으로 배정하고, 별도 발표팀과의 자료 공유는 9절 인터페이스로 처리한다.

## 6. 모듈 간 통신 계약 (변경 시 전원 합의 필요)

### 6.1 IMU 분류 출력 (모듈 A 내부, 다른 모듈은 소비만)

```cpp
enum LeanState { IDLE, LEAN_LEFT, LEAN_RIGHT };
LeanState classifyLean();  // 5~10ms 주기 폴링, idle/left/right 3클래스
```

### 6.2 ESPNOW 메시지 구조체 (모듈 A가 정의, 모듈 B가 그대로 사용)

`ESPNOW_Peer_Test.ino`의 기존 구조체를 확장. **이 구조체는 모듈 A/B 착수 전에 먼저 고정하고 공유 파일(`firmware/shared/protocol.h`)로 배포한다.**

```cpp
struct EspNowMessage {
  uint32_t magic;
  uint8_t  version;
  uint8_t  command;      // CMD_CAPTURE_REQUEST | CMD_RESULT_REPLY
  uint8_t  vehicleFound; // 결과 회신 시에만 유효 (0/1)
  uint32_t sequence;
};
```

### 6.3 카메라 분류 함수 시그니처 (모듈 C가 제공, 모듈 A/B가 소비)

```cpp
struct VisionResult { bool vehicleFound; float confidence; uint32_t inferenceMs; };
VisionResult classifyFrame(camera_fb_t* frame);  // 96x96 downsample, EI 모델 호출
```

이 시그니처만 지키면 모듈 C가 모델을 몇 번을 재학습해도 A/B 코드는 수정 불필요.

## 7. 저장소 / 협업 방식

- **GitHub 공유 저장소** 1개. 현재 로컬 디렉토리는 git 저장소가 아니므로 `git init` 후 원격 연결부터 시작.
- 폴더 구조: `firmware/master/`, `firmware/slave/`, `firmware/ble_web/`, `firmware/shared/`(공용 프로토콜 헤더), `vision_harness/`(기존), `dataset/`(기존).
- 각자 기능 브랜치에서 작업 → PR로 병합. `firmware/shared/protocol.h` 변경은 모듈 A 담당자 승인 필수(다른 모듈이 이를 소비하므로).
- 이 문서(PDR)가 모듈 간 계약서 역할 — 계약 변경 시 이 문서를 먼저 갱신하고 팀에 공지.

## 8. 개발 로드맵 — 언제 중단돼도 발표 가능한 체크포인트 구조

원칙: **각 체크포인트는 그 자체로 독립된 데모이자 증거자산(evidence artifact)이다.** 다음 단계에서 실패해도 마지막으로 성공한 체크포인트의 커밋으로 돌아가면 발표 가능한 상태가 유지된다. 각 단계는 유닛 테스트(수용 기준)를 통과해야 다음 단계로 진행한다.

별도 슬라이드 팀은 아래 표의 **증거자산** 칸만 보고 작업한다 — 개발 진행 상황과 무관하게 그 시점까지 확보된 자산으로 슬라이드를 채울 수 있다.

| 단계 | 내용 | 유닛 테스트 (수용 기준) | 중단 시 데모 | 증거자산(슬라이드팀 전달) |
|---|---|---|---|---|
| **0. 환경 구축** | arduino-cli/Edge Impulse 계정, GitHub repo init, 보드 2대 부팅 | 두 보드 모두 시리얼에 부팅 로그 출력 | "우리 하드웨어가 켜진다" | 부팅 로그 스크린샷 |
| **1. 하드웨어 구성** ✅ 2026-08-05 | 1.1 마스터 BNO055 I2C 연결+값 출력<br>1.2 슬레이브 카메라 캡처 테스트<br>1.3 마스터 부저(`tone()`)+네오픽셀(WS2812, 직접 RMT 드라이버) 단위 테스트, 슬레이브 내장 LED 단위 테스트 | 각 부품이 개별 스케치에서 정상값/동작 확인, 네오픽셀 색상 지정 점멸 확인 | "센서·카메라·부저·LED(네오픽셀 포함) 각각 단독 작동" | `checkpoint1_hw_test.ino` 5/5 통과(IMU `\|g\|=9.80`), 네오픽셀 흰색→초록3회→주황5회→노랑5회→파랑 하트비트 육안 확인됨 |
| **2. 통신 (ESPNOW)** ✅ 2026-08-05 | 2.1 MAC 상호 등록 + 단방향 전송<br>2.2 왕복(요청→응답) + 지연 측정 | 왕복 성공률 및 지연(ms) 로그로 기록 | "두 보드가 무선으로 대화한다" | `ESPNOW_Peer_Test`로 양방향 통신 확인됨. 마스터 `1C:DB:D4:74:49:E0`(COM13) ↔ 슬레이브 `AC:27:6E:A8:47:80`(COM14), `TEAM_CONFIG.md` 참조 |
| **3. IMU 학습 (가속도 센서 학습)** 🔶 데이터수집 완료 2026-08-05 | 3.1 좌/우 기울임 데이터 수집(팀원 반복 동작)<br>3.2 EI 학습(idle/left/right)<br>3.3 온보드 배포+실시간 분류 | 실내 반복 테스트에서 오분류 없음 | "카메라 없이도 몸을 기울이면 보드가 방향을 안다" | idle/left/right 각 50개(총 150개) EI 프로젝트 업로드 확인(`imu_harness/collect_and_upload_imu.py`), 학습·배포는 다음 단계 |
| **4. 턴시그널 (경보 로직 통합)** | 4.1 IMU 트리거→ESPNOW→반대쪽 LED 강제 on(카메라 결과 없이 "차량 있음"으로 가정)<br>4.2 로컬(마스터 자기 쪽) 경로 동일 검증<br>4.3 오탐 임계값 튜닝 | 마스터-슬레이브 왕복 체감 지연 없음(목표 <1초), 오탐 없음 | **사실상의 최소 완결 제품** — 기울이면 방향지시등처럼 경보가 뜬다 | end-to-end 지연 실측, 반복 성공률 |
| **5. 비전 학습** | 5.1 실외 차량/배경 데이터 수집<br>5.2 학습+온보드 배포(양쪽 보드 공용 모델)<br>5.3 캐스케이드 통합(트리거 시에만 캡처+분류로 경보 게이팅) | 6절 MVP 수용 기준 전체 통과 | **최종 완전체 데모** | 정확도, 추론시간(ms), end-to-end 영상 |
| **6. BLE/웹 로깅 (보너스)** | 폰 연결 시 로그 전송, 데모 대시보드 | 없어도 무방(스트레치 목표) | 있으면 보너스 시연, 없어도 1~5단계는 항상 살아있음 | 있으면 스크린샷 |

**중단 대응 원칙**: 각 단계 완료 시 `git tag`로 체크포인트를 명시적으로 남긴다(예: `checkpoint-1-hardware`, `checkpoint-4-turnsignal`). 발표 직전 무엇이 깨지면 가장 최근 안정 태그로 즉시 롤백 후 발표.

## 9. 슬라이드 팀 인터페이스

별도 팀이 HTML 슬라이드 덱을 준비 중이므로, 개발 진행 상황을 슬라이드 팀이 실시간으로 쫓아다니지 않아도 되도록 8절 표의 "증거자산" 칸을 완료 즉시 공유 폴더(`team-hub/downloads/` 또는 GitHub repo 내 `evidence/` 폴더)에 올리는 것을 각 체크포인트의 완료 조건에 포함한다. 슬라이드 팀은 개발 코드를 직접 볼 필요 없이 이 증거자산만으로 작업 가능하다.

## 10. 통신 설계 — 4단계 캐스케이드 (변경 없음)

| 단계 | 조건 | 내용 | 라이브 데모 취급 |
|---|---|---|---|
| 1. 엣지 단독 | 항상 | IMU→카메라→부저/LED, 폰 없이도 완결 | **메인 데모, 항상 동작해야 함** |
| 2. 폰 연결 | BLE 페어링 시 | 웹페이지 로그 전송(모듈 E) | 있으면 보너스, 없어도 1번은 살아있음 |
| 3. 인터넷 연결 | 폰이 인터넷 되어있을 때 | Edge Impulse REST API 재학습 트리거 | **라이브 트리거 안 함**, 사전 실행 결과만 슬라이드에 |
| 4. 헬멧 재연결 | 폰이 다시 헬멧에 연결될 때 | 재학습 모델 OTA 재배포 | **라이브 트리거 안 함**, "이미 성공한 증거"만 제시 |

## 11. MVP 수용 기준 (최종)

- [ ] 마스터 보드가 좌/우 기울임을 오분류 없이 구분한다(실내 반복 테스트)
- [ ] 트리거된 쪽 카메라가 1프레임 캡처 후 차량 유무를 분류한다
- [ ] 기울이면(차량 유무 무관) 마스터 네오픽셀이 해당 방향 색상으로 즉시 점등(방향지시등)
- [ ] 차량 감지 시에만 마스터 부저가 단일 비프(위험 경보, 네오픽셀과 독립)
- [ ] 마스터-슬레이브 왕복이 체감 지연 없이 동작한다(목표 <1초)
- [ ] 폰 없이도 전체 플로우가 완결된다

## 12. 주요 위험과 대응 (변경 없음, `PDR_헬멧_사각지대_경고시스템.md` 12절과 동일)

| 위험 | 대응 |
|---|---|
| IMU 오탐 | 실측 데이터로 임계값/윈도우 튜닝 |
| 실외 차량 데이터 수집 로지스틱스 | 모듈 D가 일정 초반 배정 |
| ESPNOW 왕복 지연/패킷 손실 | 기존 검증 코드(`ESPNOW_Peer_Test`) 재사용 |
| 발표장 소음으로 부저 안 들림 | LED 병행 표시로 시각적 보완 |
| 실도로 라이브 데모 불가 | 실내 재현 + 소품 차량 |

## 13. 참고 문서

- `PDR_헬멧_사각지대_경고시스템.md` — 기능 설계 원본(브레인스토밍 근거 포함)
- `TOPIC_SELECTION_RECORD.md`, `COURSE_UNDERSTANDING_AND_WINNING_STRATEGY.md` — 배경/전략
- `ESPNOW.md`, `ESPNOW_Peer_Test/`, `ESPNOW_Ring_Node/` — 통신 검증 자료
- `PROJECT_RECORD.md` — 카메라 온디바이스 분류 실측치(180ms) 근거
- `ESP32_Wearable_Hardware_Spec.md`, `하드웨어 명세서.md` — 핀맵/PSRAM 주의사항
