# ESP32-S3 시계열 데이터 기반 웨어러블 프로젝트 하드웨어 명세서

## 1. 프로젝트 개요
본 프로젝트는 ESP32-S3 마이크로컨트롤러(MCU)와 다중 I2C 센서를 활용하여 착용자 주변 환경 및 모션 시계열 데이터(Time-Series Data)를 수집하고, Edge Impulse 기반의 TinyML 모델을 통해 온디바이스(On-device) 추론을 수행하는 웨어러블 시스템 구축을 목표로 합니다.

---

## 2. 핵심 하드웨어 구성 요소

### 2.1. 메인 컨트롤러 (MCU)
*   **보드 모델**: ESP32-S3 (XIAO 폼팩터 등 소형 웨어러블용)
*   **장착 위치**: `U1`
*   **주요 특징**: 
    *   Edge Impulse AI 모델 추론을 위한 충분한 연산력 제공.
    *   시계열 데이터 모델 텐서(Tensor Arena) 할당을 위한 **외부 PSRAM (OPI/QSPI) 활성화 필수**.

### 2.2. 시계열 데이터 수집 센서부 (I2C 통신)
모든 센서는 하나의 I2C0 버스(병렬 연결)를 공유하여 핀 소모를 최소화합니다.

| 분류 | 부품명 | 장착 포트 | I2C 주소 | 데이터 특징 (시계열) | 비고 |
| :--- | :--- | :---: | :---: | :--- | :--- |
| **모션/자세** | `GY-BNO055` (9축 IMU) | **H7** | `0x29` | 가속도, 자이로, 지자기의 초당 변화량 | 웨어러블 모션 인식의 핵심 |
| **환경/대기** | `BME280` (온습도/기압) | **H4** | `0x76` | 고도 변화, 온도 및 습도 변화 트렌드 | 기압차를 이용한 층수 이동 감지 가능 |
| **환경/조도** | `BH1750` / `GY-302` | **H5** | `0x23` | 착용자의 실내외 위치 및 환경광 변화 | **ADDR 핀 미연결(NC) 필수** |

### 2.3. 디스플레이부
*   **모델**: 원형 디스플레이 (Round Display)
*   **목적**: 수집된 시계열 데이터의 실시간 플로팅(Plotting) 및 Edge AI 추론 결과(예: 행동 인식 상태, 이상 감지) 시각화 피드백.

---

## 3. 시스템 핀맵 및 인터페이스 규격

### 3.1. I2C 통신 라인 (I2C0 버스)
*   **SDA (Serial Data)**: `GPIO 5` (XIAO `D4` 호환)
*   **SCL (Serial Clock)**: `GPIO 6` (XIAO `D5` 호환)
*   **통신 방식**: 마스터(ESP32-S3)와 슬레이브(다중 센서) 간의 병렬 구조.

### 3.2. 포트 사용 가이드라인 (확장 보드 기준, SideEye 프로젝트에는 미적용)
*   **사용 가능 포트 (정상 통신 확인)**: `H1`, `H2`, `H4`, `H5`, `H7`
*   **사용 불가 포트 (사용 금지)**: `H3`, `H6`
*   **포트 장애 시 대처**: H4, H5, H7 포트 불량 시 여유 포트인 **H1** 또는 **H2**로 센서 이동 후 테스트 권장.
*   **SideEye 프로젝트 배선은 확장보드 대신 점프선 직결**을 사용한다(PCB 미사용, 확정). 마스터 보드 실제 핀 배정은 `PDR_SideEye.md` 4절 참조: SDA=D4/GPIO5, SCL=D5/GPIO6, 부저=D0/GPIO1, 네오픽셀 DIN=D1/GPIO2.

---

## 4. 조립 및 개발 시 핵심 주의사항 (Troubleshooting)

1.  **I2C 주소 충돌 및 인식 불량 방지**
    *   조도센서(`BH1750`)의 ADDR 핀은 절대 VCC/GND에 연결하지 말고 **플로팅(Floating, NC)** 상태를 유지해야 `0x23` 주소가 고정됩니다.
    *   센서 연결 후 I2C Scanner 코드를 업로드하여 `0x23`, `0x29`, `0x76` 3개의 주소가 시리얼 모니터에 모두 뜨는지 가장 먼저 확인해야 합니다.
2.  **메모리(Arena Size) 이슈 대비**
    *   시계열 데이터(Window Size)를 쌓고 추론할 때 RAM 부족(Guru Meditation Error) 현상이 빈번합니다. Arduino IDE나 PlatformIO 설정에서 **PSRAM: Enabled** 옵션을 반드시 켜야 합니다.
3.  **웨어러블 전력 소모 관리**
    *   센서를 24시간 폴링(Polling)하면 배터리가 급감합니다. 특정 센서(예: IMU)를 인터럽트(Interrupt) 핀으로 설정하여 움직임이 있을 때만 ESP32-S3를 슬립 모드에서 깨우는 방식(Wake-on-Motion) 도입을 고려해야 합니다.
