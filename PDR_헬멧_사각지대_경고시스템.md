> **최신판**: 제품명 SideEye, 팀 분업/로드맵/발표연동 포함한 확정 PDR은 `PDR_SideEye.md` 참조. 이 문서는 기능 설계 원본(배경 근거)으로 유지.

# PDR: 자전거·킥보드 헬멧 사각지대 경고 시스템 MVP

> `TOPIC_SELECTION_RECORD.md`의 브레인스토밍 결과로 확정된 최종 설계 문서. 탐색 과정(거절된 주제, 15개 리서치 조사 요약)은 그 문서를 참조하고, 이 문서는 실제로 만들 것만 다룬다.

---

## 1. 목적

자전거·킥보드 주행 중 차선 변경/조향을 위해 몸을 좌우로 기울이는 순간, 그 방향 뒤쪽 사각지대에 차량이 있는지 온디바이스로 즉시 확인하고 경고한다. 인터넷 없이도 항상 동작하는 것이 최우선이며, 폰·인터넷이 있으면 기능이 단계적으로 풍부해진다.

## 2. 한 줄 정의

헬멧 좌우에 XIAO ESP32S3 Sense를 하나씩 부착해 IMU로 몸의 기울임(조향 의도)을 감지하는 즉시 해당 방향 카메라가 차량 유무를 확인하고, LED 점멸과 부저 소리로 경고하는 **엣지 우선 + 단계적 연결성** 웨어러블.

## 3. 범위

### 3.1 48시간 MVP에 포함

- 좌/우 각 1대(총 2대)의 XIAO ESP32S3 Sense (BNO055는 그중 한 대 — 마스터 보드 — 에만 배선)
- 마스터 보드: IMU 기울임 감지 + 자기 카메라 캐스케이드 + 부저(1개) + ESPNOW 송수신
- 슬레이브 보드: ESPNOW 트리거 수신 + 자기 카메라 캐스케이드 + LED + 결과 회신
- 방향지시등 LED 점멸(5회, 각 보드 로컬)
- 부저 소리로 좌/우 구분(음높이 또는 패턴 차이)
- (시간 남으면) BLE로 폰 웹페이지 연결 → 주행 종료 후 데이터 로그 전송

### 3.2 이번 MVP에서 제외

- 샥즈 이어폰으로의 실시간 오디오 스트리밍 (ESP32-S3가 클래식 블루투스/A2DP 미지원 — 물리적으로 불가)
- 클라우드 재학습 + OTA 자동 업데이트의 **라이브 데모 트리거** (구현은 하되 사전 실행 결과를 증거로만 제시)
- Round Display 사용 (필수 아님, 실패해도 프로젝트에 영향 없어야 함)
- 실제 도로 주행 라이브 데모 (실내 재현으로 대체)
- Raspberry Pi 5/3B, OAK-D 스테레오 카메라 (특강 취지 이탈)

### 3.3 다음 단계 후보 (발표 슬라이드의 "로드맵"으로만 제시)

- 폰 웹페이지 + Web Audio 좌우 패닝으로 샥즈 이어폰 서라운드 경고(폰이 오디오 브릿지 역할)
- 인터넷 있을 때 Edge Impulse REST API로 수집 데이터 재학습
- 헬멧-폰 재연결 시 재학습된 모델 OTA 자동 배포

## 4. 학습 요소 매핑 (특강 평가 기준과의 대응)

| 특강 핵심 요소 | 이 프로젝트에서 |
|---|---|
| 웨어러블 | 헬멧 부착형, 좌우 2노드 |
| 추론모델 | IMU 기울임 분류(idle/left-lean/right-lean) + 카메라 차량 분류(vehicle/background) |
| 엣지컴퓨팅 | 캐스케이드 전체가 온디바이스, 외부 의존 없이 항상 동작 |
| 절전기능 | 카메라는 트리거될 때만 구동(상시 미촬영), IMU만 상시 저전력 감시 |
| 통신 | ESPNOW(보드간, 검증됨) + BLE(폰, 보너스) |

## 5. 하드웨어 구성

| 보드 | 위치 | 부착 부품 |
|---|---|---|
| 마스터 | 헬멧 한쪽(예: 오른쪽) | XIAO ESP32S3 Sense, BNO055(IMU), 부저 1개, LED(내장) |
| 슬레이브 | 헬멧 반대쪽(왼쪽) | XIAO ESP32S3 Sense, LED(내장) |

- 팀원 5명 전원 동일 하드웨어 보유 → XIAO Sense 재고 문제 없음(1인분만 있어도 2대 확보 가능)
- 카메라는 각자 바깥쪽(주행 방향 옆)을 향하도록 장착 — 물리적 배치 문제 없음(수어 인식안에서 겪었던 "자기 손을 찍어야 하는" 문제 없음)

### 5.1 연결 전제

- 두 보드는 같은 ESPNOW 채널 사용, MAC 주소 상호 등록 필요(`ESPNOW_Peer_Test` 코드 재사용)
- 부저는 마스터 보드 GPIO에 직결(수동 피에조, `tone()` 함수로 주파수 제어)
- 폰 연동 시 BLE는 마스터 보드가 담당(4부 notify 패턴 재사용)

## 6. 데이터 설계

### 6.1 IMU 시계열 (마스터 보드)

- BNO055 자이로+선형가속 6축, idle/shake 검증 때와 동일한 방식으로 "정지/좌기울임/우기울임" 3클래스 학습
- 팀원이 직접 좌우로 몸을 기울이며 데이터 반복 수집(반려동물처럼 협조 불가능한 대상 아님 — 리스크 낮음)

### 6.2 카메라 (양쪽 보드 공용 모델)

- 96x96(또는 기존 검증된 해상도) "차량 있음 / 배경(차량 없음)" 2클래스, 필요시 차종 세분화는 시간 남으면 확장
- 실외에서 지나가는 차량을 촬영해 데이터 수집(도로/주차장 접근 필요 — 일정에 반영)

## 7. 캐스케이드 및 경보 로직

### 7.1 원칙

- IMU 분류(idle/left/right)는 마스터 보드에서 상시 저전력으로 계속 돈다(5~10ms)
- idle이 아닌 결과가 나올 때만 해당 방향 카메라 파이프라인이 깨어난다(트리거 방식, 상시 촬영 아님)
- 몸은 동시에 좌우로 기울어질 수 없으므로, 한 시점에 트리거되는 카메라는 항상 하나뿐 — 부저 1개로 방향 구분에 충돌 없음

### 7.2 상태 흐름

```
[마스터] IMU 상시 분류(idle / left-lean / right-lean)
   │
   ├─ left-lean 감지
   │     └─ 마스터 자신이 왼쪽에 있다면: 자기 카메라 캡처+분류
   │     └─ 슬레이브가 왼쪽에 있다면: ESPNOW로 "촬영" 명령 전송
   │           └─ 슬레이브: 카메라 캡처+분류 → 자기 LED 점멸(차량 있을 때)
   │                 └─ 결과를 ESPNOW로 마스터에 회신
   │
   ├─ right-lean 감지 (반대 방향, 위와 대칭)
   │
   └─ 결과 수신(로컬 또는 ESPNOW 회신) → 차량 있음이면:
         - 마스터 부저: 좌/우에 따라 다른 음높이(예: 왼쪽=저음, 오른쪽=고음) 또는 다른 비프 횟수로 재생
         - 해당 보드 LED 5회 점멸(방향지시등처럼)
```

### 7.3 ESPNOW 메시지 구조 (안)

`ESPNOW_Peer_Test.ino`의 `EspNowMessage` 구조체를 확장:

```cpp
struct EspNowMessage {
  uint32_t magic;
  uint8_t  version;
  uint8_t  command;      // CMD_CAPTURE_REQUEST | CMD_RESULT_REPLY
  uint8_t  vehicleFound; // 결과 회신 시에만 유효 (0/1)
  uint32_t sequence;
};
```

기존 터치 온/오프 페이로드를 "촬영 요청/결과 회신"으로 바꾸는 정도라 구조 변경 최소.

## 8. 경보 정책

| 상황 | LED | 부저 |
|---|---|---|
| 기울임 감지, 차량 없음 | 해당 방향 보드 LED 점멸 없음(또는 1회 짧게) | 없음 |
| 기울임 감지, 차량 있음 | 해당 방향 보드 LED 5회 점멸(방향지시등) | 마스터 부저, 방향별 다른 음/패턴 |
| 오탐 방지 | IMU 임계값 튜닝으로 일상적인 상체 흔들림과 의도적 기울임 구분(실측 데이터로 조정) | — |

## 9. 통신 설계 — 4단계 캐스케이드

| 단계 | 조건 | 내용 | 라이브 데모 취급 |
|---|---|---|---|
| 1. 엣지 단독 | 항상 | IMU→카메라→부저/LED, 폰 없이도 완결 | **메인 데모, 항상 동작해야 함** |
| 2. 폰 연결 | BLE 페어링 시 | 웹페이지로 서라운드 경고(Web Audio 좌우 패닝, 샥즈로 재생) + 주행 종료 후 로그 전송 | 있으면 보너스 시연, 없어도 1번은 살아있음 |
| 3. 인터넷 연결 | 폰이 인터넷 되어있을 때 | Edge Impulse REST API로 수집 데이터 재학습 트리거 | **라이브 트리거 안 함**(학습 job 시간 소요), 사전 실행 결과를 슬라이드에 |
| 4. 헬멧 재연결 | 폰이 다시 헬멧에 연결될 때 | 재학습된 모델을 OTA로 헬멧에 재배포 | **라이브 트리거 안 함**, "이미 한 번 성공한 증거"만 제시 |

## 10. 확인된 기술 팩트 (제약)

- ESP32-S3는 BLE만 지원, 클래식 블루투스(A2DP) 미지원 → 상용 블루투스 이어폰(샥즈 오픈런 프로2 포함)에 직접 오디오 스트리밍 불가. 2단계는 반드시 폰을 오디오 브릿지로 거쳐야 함
- ESPNOW는 이 저장소에 이미 검증 코드 존재(`ESPNOW_Peer_Test`), 페어링 불필요, 데이터링크 계층이라 지연 낮음
- 카메라 온디바이스 분류: DSP 5ms + 분류 172~175ms ≈ 총 180ms(기존 프로젝트 실측), 트리거 방식이라 상시 스트리밍이 아니므로 지연 체감 없음
- Edge Impulse 무료 티어 학습 job 1회 최대 20분 캡

## 11. MVP 수용 기준

- [ ] 마스터 보드가 좌/우 기울임을 오분류 없이 구분한다(실내 반복 테스트)
- [ ] 트리거된 쪽 카메라가 1프레임 캡처 후 차량 유무를 분류한다
- [ ] 차량 감지 시 해당 방향 LED 5회 점멸 + 마스터 부저가 방향에 맞는 소리를 낸다
- [ ] 마스터-슬레이브 왕복(반대쪽 트리거 → 결과 회신)이 체감 지연 없이 동작한다(목표 <1초)
- [ ] 폰 없이도 전체 플로우가 완결된다(2단계 실패해도 1단계는 항상 동작)

## 12. 주요 위험과 대응

| 위험 | 대응 |
|---|---|
| IMU 오탐(일상 움직임을 조향으로 오인) | 실측 데이터로 임계값/윈도우 튜닝, 명확히 구분되는 큰 동작으로 클래스 정의 |
| 실외 차량 데이터 수집 로지스틱스(날씨·시간) | 개발 일정 초반에 배정, 도로 접근 가능한 시간대 미리 확보 |
| ESPNOW 왕복 지연/패킷 손실 | 기존 테스트 코드가 성공 콜백·수신 검증 로직 포함 — 그대로 재사용 |
| 부저 방향 구분이 청중에게 안 들릴 수 있음(발표장 소음) | 음높이 차이를 크게, LED 점멸과 병행해 시각적으로도 구분 가능하게 |
| 실제 도로 라이브 데모 불가 | 실내에서 몸 기울임 재현 + 소품 차량으로 축소 데모, 사전 녹화 영상 보조 |

## 13. 48시간 구현 순서 (초안)

1. (0~2h) 마스터/슬레이브 보드 확정, ESPNOW MAC 상호 등록 및 기존 테스트 코드로 왕복 통신 확인
2. (2~4h) BNO055 좌/우 기울임 3클래스 데이터 수집·학습(팀원이 직접 시연)
3. (4~8h) 실외 차량 이미지 수집(양쪽 카메라 공용 모델) + 학습
4. (8~14h) 캐스케이드 통합: IMU 트리거 → 로컬/ESPNOW 원격 카메라 캡처 → 결과 회신 → LED/부저
5. (14~18h) 오탐 튜닝, 부저 음 패턴 확정, 안정성 테스트 반복
6. (18~22h) (시간 되면) BLE 폰 웹페이지 로깅 붙이기
7. (남은 시간) 발표자료 작성 + 실내 데모 리허설, 버퍼

## 14. 열린 결정 사항

- [ ] 카메라 클래스를 "차량 있음/없음" 2클래스로 할지, 차종(자동차/트럭/오토바이 등)까지 세분화할지
- [ ] 부저 방향 인코딩 방식 최종 확정(음높이 vs 비프 횟수 vs 둘 다)
- [ ] 발표 프레이밍: "실전 안전 문제 해결형" vs 재미 요소 가미 여부

## 15. 근거 및 참고

- `TOPIC_SELECTION_RECORD.md` — 전체 브레인스토밍 과정, 거절된 8개 주제와 사유, 15개 리서치 에이전트 조사 원본 요약
- `ESPNOW.md`, `ESPNOW_Peer_Test/`, `ESPNOW_Ring_Node/` — 보드 간 통신 기존 검증 자료
- `PROJECT_RECORD.md` — 카메라 온디바이스 분류 실측치(180ms) 근거
